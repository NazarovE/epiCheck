<!-- res/xml/network_security_config.xml -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">appfondsod.ru</domain>
        <trust-anchors>
            <!-- Доверяем только системным сертификатам -->
            <certificates src="system" />
            <!-- Если нужно добавить свой (не рекомендуется для прода) -->
            <!-- <certificates src="@raw/custom_cert" /> -->
        </trust-anchors>
    </domain-config>
</network-security-config>